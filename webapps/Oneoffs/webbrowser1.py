import webbrowser

webbrowser.get(using='chrome')
url = 'http://docs.python.org/'
# Open URL in a new tab, if a browser window is already open.
webbrowser.open_new_tab(url)

webbrowser.open(url, new=0, autoraise=True)

# Okaym we are going to need to think about how we arte going to perform the functions in the sheet. firstly we are
# going to be exampining the sheet, greab a cell, and then you are goin gto enter it into the section on the page 



